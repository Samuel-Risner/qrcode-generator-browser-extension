import { CorrectLevel } from "./types";

const settings =  {
    default: {
        correctLevel: 0 as CorrectLevel,
        colorDark: "#000000",
        colorLight: "#ffffff"
    }
}

export default settings;
